Click-through recreation of the Photo Widget Android app: onboarding → home screen (widget on wallpaper) → widget customization (frame shape, caption, auto-refresh). Click the widget on the home screen to open customization; use back to return.

Screens compose the design system's core/forms/navigation components (Button, IconButton, Radio, Switch, Input, Tabs, Card) — no bespoke UI. Built from the product description only (no codebase/Figma attached); see root readme.md for caveats.
