Text input with sunken well styling, matching the calm/soft app aesthetic.

```jsx
<Input label="Caption" placeholder="Add a caption…" icon="edit" value={v} onChange={setV} />
```

Focus swaps the hairline border to terracotta; `error` renders a message below in the danger tone.
