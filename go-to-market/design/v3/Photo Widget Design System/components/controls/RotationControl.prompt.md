Drag-to-rotate dial with step buttons and a live degree readout — snaps to 45° increments around the full 360° circle (e.g. tilting the widget's photo to a clean angle).

```jsx
<RotationControl value={angle} onChange={setAngle} />
```
