Anchor pad — follows the standard reference-point-picker pattern (Photoshop/Figma-style): each cell shows a directional arrow toward its position (compass icons), center shows a dot. Selected cell fills with the accent color; hover shows a raised tint.

```jsx
<AlignmentPad value={anchor} onChange={setAnchor} />
```
