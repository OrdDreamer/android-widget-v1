Compact action/preset chip — quick picks like refresh intervals or quick actions, distinct from `Tag`'s persistent filter role.

```jsx
<Chip icon="schedule" selected={v==='1h'} onClick={() => setV('1h')}>1 hour</Chip>
```
