A `Slider` paired with a row of one-tap preset values — e.g. common rotation angles or refresh intervals.

```jsx
<SliderPresets label="Rotation" min={0} max={315} value={angle} unit="°" presets={[0,45,90]} onChange={setAngle} />
```
