Full-width inline error, sits in the form flow (unlike `Toast`, which floats) — e.g. "Couldn't load this photo."

```jsx
<InlineErrorBanner message="Couldn't load this photo." onDismiss={clear} />
```
