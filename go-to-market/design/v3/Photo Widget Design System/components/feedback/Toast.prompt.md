Brief bottom-anchored confirmation, e.g. "Widget updated".

```jsx
<Toast message="Widget updated" tone="success" icon="check_circle" />
```

Fades + slides up gently on entry (no bounce — matches the app's calm motion). Ink-dark by default; sage for success, red for errors.
