Linear progress bar — e.g. photo upload/sync. Determinate fills to `value`%; `indeterminate` shows a calm sliding segment (no bounce).

```jsx
<ProgressIndicator value={64} />
<ProgressIndicator indeterminate />
```
