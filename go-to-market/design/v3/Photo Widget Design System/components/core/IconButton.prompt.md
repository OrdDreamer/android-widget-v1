Circular icon-only button for toolbars and headers.

```jsx
<IconButton icon="settings" variant="ghost" label="Settings" />
```

Variants: `ghost` (transparent, hover tint), `filled` (white with soft shadow, floating over photos), `accent` (terracotta-tinted). Sizes `sm`/`md`/`lg` map to 32/40/48px circles.
