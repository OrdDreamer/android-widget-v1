Generic content container — the base surface for photo preview panels, settings groups, and list rows.

```jsx
<Card variant="default" padding="md">…</Card>
```

`default` sits on the cream app background with a hairline border + faint shadow; `raised` is pure white with a stronger shadow (use sparingly, for floating elements); `sunken` recesses into the background with no shadow (use for input wells).
